# Local SQLite caches (should never be committed)
CrimsonGameMods/crimson_data.db
*.db
*.db-journal
*.db-shm
*.db-wal

# Docs (local only)
docs/

# Sibling project (out of scope for this repo)
crimson-rs/